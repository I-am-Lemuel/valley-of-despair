import { StyledFooter } from "./StyledFooter";

export default function Footer() {
    return (
        <StyledFooter>
            <footer>
                <p>C</p>
            </footer>
        </StyledFooter>

    )
}